import styled from 'styled-components'
import Header from '../header/Header'

const Nav = () => {
	return (
		<Wrapper>
			<Header />
		</Wrapper>
	)
}

export default Nav

export const Wrapper = styled.div``
