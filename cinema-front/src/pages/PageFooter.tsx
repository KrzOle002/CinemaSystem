import styled from 'styled-components'

const PageFooter = () => {
	return <Wrapper>PageFooter</Wrapper>
}

export default PageFooter

const Wrapper = styled.div`
	color: ${({ theme }) => theme.colors.white};
	width: 100%;
`
